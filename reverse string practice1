#include <stdio.h>

int main (){
char name[100];
char rename[100];
printf("Enter name:");
scanf("%s",name);
int i=0;
while(name[i]!='\0'){
    i++;
}
i--;
int j=0;

for(;i>=0;i--){
    rename[j]=name[i];
    j++;
}
rename[j]='\0';
printf("rename:%s",rename);

return 0;
}
